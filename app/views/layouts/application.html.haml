!!!
%html{:'ng-app' => "biller"}
  %head
    %title Biller
    = stylesheet_link_tag    'application', media: 'all'
    = javascript_include_tag 'application'
    = csrf_meta_tags
  %body
    .navbar.navbar-inverse.navbar-static-top
      .container-fluid
        .navbar-header
          %a.navbar-brand Biller
    - flash.each do |key, msg|
      = content_tag :div, msg, class: "alert alert-#{key}" if msg.is_a?(String)
    = yield
